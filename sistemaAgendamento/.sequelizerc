const { resolve } = require("path")

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-paths': resolve(__dirname, 'src', 'app', 'models'),
  'migations-paths': resolve(__dirname, 'src', 'database', 'migrations'),
  'seeders-paths': resolve(__dirname, 'src', 'database', 'seeders'),
}